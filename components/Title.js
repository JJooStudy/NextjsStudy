import styled from 'styled-components';

const Title = styled.h3`
    margin:20px 0 10px;
    font-size:18px;
    font-weight:normal;
    > span {
        font-size:14px;
        color:gray;
    }
`

export default Title;